import Accordion from "../components/Accordion";

const Main = ({languages}) => {
    return (
        <main>
            <Accordion languages={languages}  />
        </main>
    )
};
export default Main;   